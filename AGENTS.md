# AGENTS.md

## 목적

이 프로젝트는 `CLAUDE.md`를 기본 에이전트 지침 파일로 사용한다.

이 저장소에서 작업을 시작하기 전에 반드시 다음 순서를 따른다.

1. 먼저 `CLAUDE.md`를 읽는다.
2. `CLAUDE.md`에 정의된 모든 규칙과 우선순위를 따른다.
3. 현재 MVP 요구사항과 구현 범위를 확인하기 위해 `BLOG_MVP_SPEC.md`를 읽는다.
4. `CLAUDE.md`에 이미 정의된 규칙을 이 파일에서 중복해서 해석하거나 다시 정의하지 않는다.

---

## 기준 문서 우선순위

다음 우선순위를 따른다.

1. 현재 세션에서 사용자가 명시적으로 전달한 지시
2. `CLAUDE.md`
3. `BLOG_MVP_SPEC.md`
4. 현재 프로젝트 코드와 기존 구조
5. `README.md`

이 파일과 `CLAUDE.md`의 내용이 충돌하는 경우에는 반드시 `CLAUDE.md`를 따른다.

---

## 중요 원칙

`AGENTS.md`는 의도적으로 최소한의 내용만 유지한다.

다른 코딩 에이전트와의 호환성을 위해 반드시 필요한 내용이 아니라면 이 파일에 프로젝트 규칙을 추가하지 않는다.

프로젝트 전반의 다음 내용은 모두 `CLAUDE.md`에서 관리한다.

- 개발 원칙
- 구현 제한사항
- 기술 스택
- 작업 우선순위
- 검증 방식
- 코딩 규칙
- 배포 원칙
- 프로젝트 운영 지침

즉, 프로젝트의 에이전트 지침을 수정해야 할 경우 기본적으로 `CLAUDE.md`만 수정한다.
